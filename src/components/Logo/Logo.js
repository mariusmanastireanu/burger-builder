import React from "react";
import burgerLogo from "../../images/burger-logo.png"
import classes from "./Logo.css"

const logo = (props) => (

    <div className="Logo">
        <img src={burgerLogo}/>
    </div>

);

export default logo;